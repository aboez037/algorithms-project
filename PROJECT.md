# PROJECT.md — وصف مشروع إدارة المهام والمشاريع (كانبان)

> هذا الملف هو المرجع الأساسي لأي مطوّر أو لـ Cursor AI عند العمل على المستودع.
> اقرأه قبل كتابة أي كود. للتوزيع اليومي راجع أيضاً: `دليل_الأعضاء.md` و `خطة_التنفيذ.md`.

---

## 1) ما هو المشروع؟

نظام مبسّط لإدارة **المشاريع والمهام** مع لوحة **Kanban**، مع تطبيق مفهومين خوارزميين فقط على مشاكل حقيقية داخل النظام:

1. **Comparison Sorting** — ترتيب المهام (Merge Sort + Quick Sort يدوياً)
2. **Divide and Conquer** — حساب إجمالي الجهد/الساعات من شجرة المهام بشكل عودي

الهدف الأكاديمي: إثبات أن الخوارزميات أدوات عملية لمدير المشروع، وليست نظريات مجردة.

| البند | القيمة |
|---|---|
| المادة | تحليل وتصميم الخوارزميات |
| المستودع | https://github.com/aboez037/algorithms-project |
| التسليم | 14-09-2026 |
| الفريق | 3 أعضاء |
| التقنية | حرة (يحددها الفريق ويوثقها هنا عند الاتفاق) |

---

## 2) المتطلبات الوظيفية (يجب أن يعمل النظام بها)

1. **إنشاء مشروع**: اسم، وصف، تاريخ بداية، تاريخ نهاية.
2. **إنشاء مهام**: عنوان، وصف، أولوية (`HIGH` / `MEDIUM` / `LOW`)، تاريخ تسليم.
3. **تقسيم هرمي**: المهمة الرئيسية = عقدة، المهام الفرعية = أبناء (`parentId`).
4. **توزيع المهام**: تعيين مهمة (أو فرعية) لأحد أعضاء الفريق المسجّلين.
5. **لوحة كانبان**: أعمدة ثلاثة — جديد / قيد العمل / منتهي — مع تغيير الحالة.
6. **Activity Log**: سجل زمني لكل تغيير حالة.
7. **تقرير بسيط**: نسبة الإنجاز الكلية + عدد المهام المنجزة لكل عضو + إجمالي الجهد.

---

## 3) المتطلبات الخوارزمية (إلزامية للتقييم)

### 3.1 Comparison Sorting

- **المشكلة**: ترتيب المهام حسب الأولوية (الأعلى أولاً) أو حسب التاريخ المتبقي (الأقرب أولاً).
- **المطلوب**: تنفيذ يدوي لـ **Merge Sort** و **Quick Sort**.
- **ممنوع**: استخدام دوال الترتيب الجاهزة (`sort`, `Collections.sort`, `Arrays.sort`, `sorted`, …) لجزء التقييم.
- **الواجهة**: عند زر «ترتيب» تُعرض القائمة مرتبة + **زمن التنفيذ بالميلي ثانية** لكل خوارزمية للمقارنة.
- **التحليل المطلوب في التوثيق**: أفضل/أسوأ حالة، وشرح `O(n log n)` مقابل حالات `O(n²)` ولماذا تتفوق إحداهما أحياناً.

### 3.2 Divide and Conquer

- **المشكلة**: حساب إجمالي الساعات/الجهد للمشروع من المهام الفرعية المتداخلة.
- **المطلوب**: دالة عودية تستقبل الجذر → تقسم للأبناء → تحسب جهد كل فرع → تدمج المجموع.
- **الفكرة**: «اقسم ثم احكم» عبر استدعاء ذاتي، وليس مجرد حلقة مسطّحة تجمع كل الصفوف بدون تقسيم شجري.
- **التحليل المطلوب**: التعقيد تقريباً `O(n)` لأنها تزور كل عقدة مرة + توضيح شجرة الاستدعاءات (Call Stack).

---

## 4) نموذج البيانات المتفق عليه

لا تغيّر أسماء الحقول بدون اتفاق الفريق.

### User (عضو)

- `id`
- `name`
- (اختياري) `email` / `password` حسب أسلوب المصادقة المختار

### Project (مشروع)

- `id`
- `name`
- `description`
- `startDate`
- `endDate`

### Task (مهمة)

| الحقل | النوع/القيم | ملاحظات |
|---|---|---|
| `id` | معرف | مطلوب |
| `title` | نص | مطلوب |
| `description` | نص | |
| `priority` | `HIGH` \| `MEDIUM` \| `LOW` | للترتيب |
| `dueDate` | تاريخ | للترتيب حسب المتبقي |
| `status` | `NEW` \| `IN_PROGRESS` \| `DONE` | للكانبان |
| `assigneeId` | معرف مستخدم أو null | للتوزيع والتقرير |
| `parentId` | معرف مهمة أو null | `null` = مهمة رئيسية |
| `estimatedHours` | رقم | لفرق تسد |
| `projectId` | معرف مشروع | مطلوب |

### ActivityLog (سجل)

- `id`
- `taskId`
- `userId` (من قام بالتغيير)
- `fromStatus`
- `toStatus`
- `timestamp`

### علاقات مختصرة

```
Project 1──* Task
User 1──* Task (assignee)
Task 1──* Task (parent → children عبر parentId)
Task 1──* ActivityLog
```

---

## 5) الفريق والأدوار

| العضو | المسؤولية |
|---|---|
| **محمد كل الناس** | المستخدمون + لوحة الكانبان + Activity Log |
| **بلال السعيد** | المشاريع + المهام + التوزيع |
| **aboez037** | الهيكل الهرمي + Merge/Quick Sort + فرق تسد + التقرير والتحليل |

فروع مقترحة:

- محمد: `feature/auth-kanban-log`
- بلال: `feature/projects-tasks-assign`
- aboez037: `feature/hierarchy-sorting-dc-report`

تفاصيل التنفيذ اليومية: انظر `دليل_الأعضاء.md`.

---

## 6) ترتيب البناء (Dependencies)

```
محمد: مستخدمون
        ↓
بلال: مشاريع + مهام + تعيين
        ↓
aboez037: شجرة + Sorting + D&C + تقرير
        ↕
محمد: كانبان + Activity Log (بعد توفر المهام)
```

---

## 7) قواعد Git

- `main` = التسليم النهائي فقط
- `dev` = دمج يومي بعد مراجعة
- كل ميزة على فرع `feature/...`
- الدمج عبر Pull Request + Code Review من زميل واحد على الأقل
- رسائل commit واضحة ومعبّرة

---

## 8) تعليمات لـ Cursor / أي مساعد AI

عند توليد أو تعديل كود في هذا المستودع التزم بما يلي:

1. اقرأ هذا الملف (`PROJECT.md`) و`دليل_الأعضاء.md` قبل اقتراح بنية كبيرة.
2. احترم نموذج البيانات أعلاه؛ لا تعيد تسمية الحقول الأساسية.
3. **لا** تستخدم ترتيب جاهز من اللغة/المكتبة لتنفيذ متطلب Sorting الأكاديمي.
4. دالة الجهد يجب أن تكون **عودية / فرق تسد** على الشجرة.
5. الكانبان له ثلاث حالات فقط: `NEW`, `IN_PROGRESS`, `DONE`.
6. اجعل التغييرات صغيرة ومرتبطة بدور العضو الذي يطلب المساعدة.
7. التقنية حرة؛ إذا لم تُحدد بعد، اقترح خياراً بسيطاً وسريعاً للتسليم واطلب تأكيداً قبل بناء هيكل ضخم.
8. لا تكتب في المستودع نسب عمل أو مقارنات «من يعمل أكثر»؛ فقط أدوار ومهام.
9. بعد الاتفاق على اللغة/الإطار، حدّث القسم 9 أدناه.

---

## 9) التقنية المختارة (يُحدَّث باتفاق الفريق)

| البند | القرار |
|---|---|
| اللغة | *(غير محدد بعد)* |
| الإطار / الواجهة | *(غير محدد بعد)* |
| التخزين | *(ذاكرة / ملف JSON / قاعدة بيانات — غير محدد بعد)* |
| طريقة التشغيل | *(تُكتب أوامر التشغيل هنا لاحقاً)* |

---

## 10) معايير القبول (Definition of Done)

المشروع جاهز عندما:

- [ ] يمكن إنشاء مشروع ومهام ومهام فرعية وتعيينها لأعضاء
- [ ] الكانبان يعمل بثلاث حالات مع سجل تغييرات
- [ ] Merge Sort و Quick Sort يعملان يدوياً مع عرض الزمن بالميلي ثانية
- [ ] فرق تسد يحسب إجمالي الجهد من الشجرة بشكل عودي
- [ ] التقرير يعرض نسبة الإنجاز ومهام/إنجاز الأعضاء
- [ ] README يشرح التشغيل + تحليل التعقيد
- [ ] يوجد تاريخ Pull Requests واضح على GitHub

---

## 11) ملفات المرجع في المستودع

| ملف | الغرض |
|---|---|
| `PROJECT.md` | وصف المشروع الكامل (هذا الملف) — مرجع Cursor والفريق |
| `دليل_الأعضاء.md` | قائمة تنفيذ لكل عضو |
| `خطة_التنفيذ.md` | خطة المراحل وقواعد التعاون |
| `README.md` | مدخل سريع للمستودع |
